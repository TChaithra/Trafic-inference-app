# Shared
IMX8_HOST=192.168.1.100  # IP of IMX8M+ board
IMX8_PORT=8001           # IMX8 service port

# Backend
BACKEND_HOST=0.0.0.0
BACKEND_PORT=8000
MAX_CONCURRENT_JOBS=2    # Worker concurrency limit
JOB_TIMEOUT=30           # Seconds per job

# Frontend
VITE_API_URL=http://localhost:8000  # Backend API base

# IMX8-specific (set on board)
MODEL_PATH=./model/model.tflite
DELEGATE_PATH=/usr/lib/libvx_delegate.so  # From eIQ toolkit
